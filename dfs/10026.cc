#include <iostream>

using namespace std;

string pic_line[101];
bool visited[101][101];

int main (void)
{
    ios_base :: sync_with_stdio (0);
    cin . tie (0); cout.tie (0);

    int N; cin >> N;
    for (int i = 1; i <= N; i++)
    {
        cin >> pic_line[i];
    }

        

    return 0;
}